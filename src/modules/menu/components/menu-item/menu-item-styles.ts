import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    menuElement: {
        color: '#efefef',
        borderRadius: 5,
        backgroundColor: '#404646',
        margin: 10,
        padding: 30
    },
    buttonText: {
        textAlign: 'center'
    }
})
